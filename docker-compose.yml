version: '3.8'
services:
  discord-bot:
    build: .
    image: discord-bot:latest
    restart: unless-stopped
    volumes:
      - ./data:/data
    logging:
      driver: awslogs
      options:
        awslogs-region: ${AWS_REGION}
        awslogs-group: /discord-bot/logs
