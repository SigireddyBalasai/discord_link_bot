codecommit_name = "discord-link-bot"
aws_region = "us-east-1"

# The SSH keypair is generated by Terraform and saved locally at `infra/.ssh/<name>.pem`.

tags = {
  ManagedBy = "Terraform"
  Project   = "discord-bot"
}
