/* Removed: EventBridge triggers; pipeline is fully automatic. */


/* All eventbridge triggers were intentionally removed — we rely on CodePipeline's Source -> Build -> Deploy flow.
   To re-enable an EventBridge-triggered build on pushes, add a new rule and role implementation. */
